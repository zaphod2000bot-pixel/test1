Flutter Greet

This is a minimal Flutter app that displays a text field and a Greet button. Pressing the button shows an alert dialog with "Hello [Name]".

How to run:
1. Install Flutter SDK: https://flutter.dev/docs/get-started/install
2. From this directory, run:
   flutter pub get
   flutter run
